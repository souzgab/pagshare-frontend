import React from 'react';
import Button from '../../components/Button';

class Home extends React.Component{
    render(){
        return (
            <div>
                <Button text="Login" />
            </div>
        );
    }
}

export default Home;
